/** * You need a document with a page and a selected rectangle with an image in it */if(app.selection[0] instanceof Rectangle){    var sourcerect = app.selection[0];    if(sourcerect.allGraphics.length > 0){            var img = sourcerect.allGraphics[0];            alert(img.itemLink.filePath);            var targetrect = img.parentPage.rectangles.add({geometricBounds:[0,0,100,100]});            targetrect.place(File(img.itemLink.filePath));                }    }